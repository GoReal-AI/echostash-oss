{
  "name": "@echostash/discovery",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Usage-anchored, language-agnostic prompt discovery: ripgrep finds LLM call sites (known SDK shapes + framework-agnostic structural anchors + optional LLM-derived custom wrappers), then resolves each prompt argument back to its source. Deterministic, with a dedicated-file/strong-string fallback.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@echostash/llm": "workspace:*",
    "@echostash/shared": "workspace:*",
    "@vscode/ripgrep": "^1.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsup": "^8.5.1",
    "typescript": "^5.7.2",
    "vitest": "^3.2.6"
  }
}
